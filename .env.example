# ==============================================================================
# Bot Configuration
# ==============================================================================

# Discord bot token (required)
DISCORD_TOKEN=""

# OpenAI API key (required)
OPENAI_API_KEY=""

# Replicate API token for image generation (optional)
REPLICATE_API_TOKEN=""

# ElevenLabs API key for text-to-speech (optional)
ELEVEN_LABS=""


# ==============================================================================
# Cloud Storage Configuration (one of R2 or S3 is REQUIRED)
# ==============================================================================
# The bot requires cloud storage for generated images. Choose either Cloudflare
# R2 (recommended) or AWS S3. Do NOT configure both - the bot will use R2 if
# both are configured.


# ------------------------------------------------------------------------------
# Option 1: Cloudflare R2 (RECOMMENDED)
# ------------------------------------------------------------------------------
# R2 offers generous free tier (10GB storage, unlimited egress) and is
# S3-compatible. Great for hobby projects and production alike.
#
# Setup steps:
# 1. Create a Cloudflare account at https://dash.cloudflare.com
# 2. Go to R2 Object Storage and create a bucket
# 3. Enable public access for the bucket (Settings > Public Access)
# 4. Create an API token with R2 read/write permissions
# 5. Fill in the values below

R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME=""
R2_ACCOUNT_ID=""
# Public URL is either your custom domain or the r2.dev URL
R2_PUBLIC_URL=""


# ------------------------------------------------------------------------------
# Option 2: AWS S3 (Alternative)
# ------------------------------------------------------------------------------
# Traditional cloud storage option. Requires an AWS account and may incur costs
# for storage and data transfer.
#
# Setup steps:
# 1. Create an AWS account
# 2. Create an S3 bucket with public read access
# 3. Create an IAM user with S3 permissions
# 4. Fill in the values below

# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# S3_BUCKET_NAME=""
# AWS_REGION="us-east-1"